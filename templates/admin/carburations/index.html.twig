{% extends 'admin.base.html.twig' %}


{% block title %} Ajouter un type de boite{% endblock %}

{% block stylesheets %}{% endblock %}
{% block body %}

    {% include 'admin/partials/navbar.html.twig'%}


    <div class="content-wrapper">
        <div class="container-fluid">
            {% include 'admin/partials/flash.html.twig'%}
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="#">Admin</a>
                </li>
                <li class="breadcrumb-item active">Liste des types de carburation</li>
            </ol>
            <a href="{{ path('admin_carburation_add') }}"><button class="btn btn-success">Ajouter un type de carburation</button></a>
            <table class="table table-striped" style="margin-top: 5px;">
                <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Type de carburation</th>
                    <th scope="col">Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for carburation in carburations %}
                    <tr>

                        <th scope="col">{{ carburation.id }}</th>
                        <th scope="col">{{ carburation.carburation }}</th>
                        <th scope="col">
                            <a href="{{ path('admin_carburation_update', {id: carburation.id}) }}">
                                <button class="btn btn-warning">modifier <i class="fa fa-edit"></i></button>
                            </a>
                            <a href="{{ path('admin_carburation_delete', {id: carburation.id}) }}">
                                <button class="btn btn-danger">supprimer <i class="fa fa-trash"></i></button>
                            </a>
                        </th>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
